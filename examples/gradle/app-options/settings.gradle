pluginManagement {
    repositories {
        mavenCentral {}
        gradlePluginPortal {}
        maven {
            url System.getProperty('user.home') + '/.m2/repository'
        }
    }
}

rootProject.name = 'app-options'

